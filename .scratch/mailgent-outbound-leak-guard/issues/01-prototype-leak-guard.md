# Prototype Outbound Leak Guard

Type: prototype
Status: resolved

## Question

How should humans configure on-device subject/body scanning before agents receive mail — built-in heuristics, custom rules, placement opt-in, and hit modes?

## Answer

HTML prototype in [`../examples/prototype-outbound-leak-guard.html`](../examples/prototype-outbound-leak-guard.html).

Shipped in SwiftUI as Grant Desk → **Privacy** tab plus Scope **Scan** toggles and Access preview. Native `Form` / `Toggle` / `DisclosureGroup` — no custom shield chip from the HTML.
